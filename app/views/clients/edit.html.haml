- content_for :title, @client.business_name ? "Editing: #{@client.business_name}" : "Editing: #{@client.contact_name}"

.ui.segment.center.aligned
  = render 'form'

= link_to 'Show', @client
\|
= link_to 'Back', clients_path
